# ZIP Inbox Run Outputs

Machine-readable run artifacts generated by the ZIP inbox runtime scripts.

Typical outputs:
- validation reports
- registry snapshots
- install plans
- archive manifests

Path pattern:
- `04_runs/zip_inbox/<project_slug>/<run_id>/`
